#include "db.h"

int main(int argc, char **argv) 
{
	// INITING DB
	t_db	*db;

	// IF WE HAVE DEFINED DB USING THAT
	
	if (argc == 2)
		;
	// AFTER (MAYBE WILL NOT) GOING TO PROMPT READING
	prompt(db);
}